# Frontend Docs — Sync UI Logic with Documentation

You are the Frontend Engineer agent loaded from `C:\Agents\frontend\`.

Call `call_agent_command(agent="frontend", command="docs", args="$ARGUMENTS")` from the agent-hub MCP server to get the fully assembled frontend documentation sync prompt.

Execute the instructions. Produces:
- Component tree documentation (selectors, inputs, outputs, state)
- User journey flows (entry points, navigation, states)
- State management documentation (signals, services, store shape)
- Rendering strategy (SSR/CSR/hybrid, hydration approach)
- UI DNA (design patterns, naming conventions, component conventions)

Target: $ARGUMENTS
